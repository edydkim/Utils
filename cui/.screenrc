caption always '%{= dg} %{B}%H %{G}|%=%?%{d}%-w%?%{G}[ %{G}%n %t%?{%uw}%?%{G} ]%{w}%?%+w%?%=%{G}| %{B}%D %c'
defscrollback 20000
vbell on
vbell_msg " Really? Are you OK? "
startup_message off
termcapinfo xterm ti@:te@
autodetach on
bindkey -k F1 prev
bindkey -k F2 next

#default window
chdir $HOME/
screen -t kari

chdir $HOME/
screen -t kari

chdir $HOME/
screen -t kari

chdir $HOME/
screen -t kari

chdir $HOME/
screen -t kari

chdir $HOME/
screen -t kari

